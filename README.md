# Webpack 5 React boilerplate

Basic configuration of React with webpack 5 in case CRA is the overhead.

## Features

- JSX
- CSS-modules
- images
- fonts
- hot reload
- airbnb ESLint

## Launch it

```bash
npm i && npm start
```